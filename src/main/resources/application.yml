spring:

cloud:
    aws:
        credentials:
          access-key: ${AWS_S3_ACCESS_KEY}
          secret-key: ${AWS_S3_SECRET_KEY}

    application:
            bucket: 
                name : myfirstbuck2726

    server:
        port: 8080